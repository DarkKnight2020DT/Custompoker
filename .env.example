NODE_ENV=production
PORT=3000
# Use a persistent mounted directory in production.
DATA_DIR=/data
# Optional explicit SQLite location. Defaults to $DATA_DIR/custom-poker.sqlite
# DATABASE_PATH=/data/custom-poker.sqlite
