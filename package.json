{
  "name": "custom-poker",
  "version": "1.1.0-beta.1",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test",
    "check": "node --check server/index.js && node --check server/db.js && node --check server/auth.js && node --check server/persistence.js && node --check public/app.js",
    "backup": "node scripts/backup.js"
  },
  "engines": {
    "node": ">=24"
  },
  "description": "Custom Poker hosted beta with accounts, groups, persistent ledgers, settlements, chat, debug bundles, and configurable poker variants.",
  "license": "UNLICENSED"
}
